# Claude Code — Business AI

Read `BUSINESS.md` first.

Use shared business memory in `projects/`, `ideas/`, `decisions/`, `research/`, `experiments/` and `meetings/`.

Use skills in `.claude/skills/` for structured analysis. Important conclusions should be recorded in files, not left only in chat.
