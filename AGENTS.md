# Codex — Business AI

Read `BUSINESS.md` first.

Use shared business memory in `projects/`, `ideas/`, `decisions/`, `research/`, `experiments/` and `meetings/`.

Use skills in `.agents/skills/` for structured analysis. Important conclusions should be written back to the shared business files.
